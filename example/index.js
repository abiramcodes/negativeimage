const pencilSketch = require('negativeimage');

// Specify the input and output paths
const inputPath = 'flower.jpeg';
const outputPath = 'black.jpg';

// Call the pencilSketch function
pencilSketch(inputPath, outputPath);
